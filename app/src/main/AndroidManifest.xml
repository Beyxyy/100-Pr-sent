<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="29" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PrezzApp"
        tools:targetApi="31">

        <!--<service
            android:name=".database.MyService"
            android:enabled="true"
            android:exported="true"></service>-->

        <activity
            android:name=".AdminActivity"
            android:exported="true"
            android:label="@string/title_activity_admin"
            android:theme="@style/Theme.PrezzApp">
        </activity>

        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.PrezzApp" >

        </activity>

        <activity
            android:name=".StudentDashboardActivity"
            android:exported="true">
        </activity>
        <activity
            android:name=".JustifyAbsenceActivity"
            android:exported="false"
            android:parentActivityName=".StudentDashboardActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".StudentDashboardActivity" />
        </activity>

        <!-- Ajout de ViewJustificationActivity -->
        <activity
            android:name=".ViewJustificationActivity"
            android:exported="false"
            android:parentActivityName=".StudentDashboardActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".StudentDashboardActivity" />
        </activity>

        <activity
            android:name=".TeacherDashboardActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".ViewJustificatifTeacherActivity"
            android:exported="true"
            android:parentActivityName=".TeacherDashboardActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".TeacherDashboardActivity" />
        </activity>

        <activity
            android:name=".TeacherCoursesActivity"
            android:exported="true"
            android:parentActivityName=".TeacherDashboardActivity" >
        </activity>

            android:exported="true" />
    </application>
</manifest>